import {defineField, defineType} from 'sanity'

export default defineType({
    name: "page",
    title: "Home",
    type: "document",
    fields: [
        defineField({
            name: "hero",
            title: "",
            type: ""
        })
    ]
})